import styled from "styled-components";

export const StyledListPost = styled.ul`
display: flex;
flex-direction: column;
gap: 1.5em;
padding: 1.2em 1em;
background-color:#f5f5f5;
height: auto;
max-width:19em;

h1{
  font-size: 18px;
  color: var(--color-primary);
}
`